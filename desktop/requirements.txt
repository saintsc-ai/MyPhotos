# Runtime dependency — Qt 6 bindings include QWebEngine + QtWidgets
# (server-manager UI) on Windows and macOS.
PySide6>=6.7

# Build-time only (run pyinstaller from the same venv to bundle to exe).
pyinstaller>=6.6
